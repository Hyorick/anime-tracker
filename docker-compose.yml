services:
  postgres:
    image: postgres:16
    container_name: anime-tracker-postgres-dev
    environment:
      POSTGRES_DB: anime_tracker
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"